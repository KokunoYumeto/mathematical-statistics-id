# Cross-lane overlap boundary

Checked: 2026-08-21  
Current task: O006 / C140 Mathematical Statistics

## Exclusive source ownership

O006 owns the Bahasa Indonesia translation and stable backend records for these
29 Random files:

- all nine files under `random/sample/` selected by the authority receipt;
- all seven selected files under `random/point/`;
- all six selected files under `random/interval/`;
- all seven selected files under `random/hypothesis/`.

The four chapter indexes are included in those counts.  Other tasks may depend
on these modules, but must not independently translate or fork them.

## O009 / D30

The active O009 task uses Random for measure-theoretic probability and
stochastic processes plus separate Žitković laboratories.  Its planning had
included Random's sample LLN/CLT pages, which overlap this lane.  O009 was sent
an explicit coordination message on 2026-08-21: use O006's chapter-5 stable
modules as prerequisites and own only disjoint convergence/process pages and
labs.  At the check boundary, O009 had exactly one target source file,
`source/theory/prob/Convergence.html`; it is outside O006's 29-file set.  No
translated target bytes were duplicated.

If O009 requires genuinely measure-theoretic convergence or limit-theorem
material beyond O006, it may select or author a distinct advanced module.  It
must not reproduce the same Random chapter-5 translation.

## B90 and B95

- The B90 task owns the Grinstead–Snell probability corpus.  Topic-level
  prerequisite overlap is expected, but there is no shared authority file or
  duplicate translation.
- The B95 task owns OpenIntro Statistics and its applied-statistics reader.
  It uses a separate repository, assets, exercises, and backend.  Topic-level
  overlap with introductory statistics does not transfer any O006 source file.

## Coordinator boundary

Task `01a01ec1-e685-70d0-b022-211396334723` owns curriculum selection and the
global human-facing hub.  O006 owns only its individual corpus repository,
reader, backend, releases, and substantial-boundary publication.

Recheck this ledger before importing any new donor chapter or bridge source.
